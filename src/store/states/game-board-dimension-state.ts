export interface GameBoardDimensionState {
    cardHeight: number,
    cardWidth: number,
    cardsPerRow: number,
    height: number,
    isPanelExpanded: boolean,
    selectedCardWidth: number,
    selectedCardHeight: number,
    width: number,
} 