import { makeStyles } from "@material-ui/core";

export const useGamePanelStyles = makeStyles((theme) => ({
    container: {
        flex: '1 1 100%',
        height: 0,
        position: 'relative'
    }
}));